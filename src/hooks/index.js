/* @flow */
export default {
  onBeforeChange: (dispatch: Function, getState: Function, action: Object) => { // eslint-disable-line
    // Runs before a router change.
  },
  onAfterChange: () => {
    // Runs after a router change.
  }
}
